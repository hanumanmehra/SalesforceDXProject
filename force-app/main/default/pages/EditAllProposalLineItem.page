<apex:page >
  <!--<apex:page StandardController="Proposal__c" extensions="EditAllProposalLineItemController" doctype="HTML-5.0">
    <script src="/soap/ajax/15.0/connection.js" type="text/javascript"></script>
    <script src="/soap/ajax/15.0/apex.js" type="text/javascript"></script>
    <script>
        function quantValid(index,valueofInput)
        {
            var tempcls = "text"+index;
            var selectValue = valueofInput;
            //alert('tempcls '+tempcls  +'selectValue '+selectValue );
            if(selectValue  < 0)
            {
                alert('sales price never be less than zero');
            }
            
        }
    </script>
    
    <style>
        .ui-state-active{
        
        border: 1px solid #fbd850;
            background: #ffffff;
            font-weight: bold;
            color: #eb8f00;
        
        }
        
        .BlockTable{width:60%!important; margin:0 auto}
        .BlockTable th{
        background: #099ad6!important;
    color: #fff!important;
    padding: 8px 10px!important;
    font-weight: bold!important;
    font-size: 13px!important;}
    
        .BlockTable td{padding:8px 10px!important}
        .BlockTable input{ border:1px solid #eee; width:100%; padding:6px 10px;box-sizing: border-box;}
    </style>
     <apex:form >         
        <apex:pageBlock id="table" >
        <apex:pageMessages />
        <apex:variable value="{!0}" var="indCount"/>
        <apex:variable var="index" value="{!1}"/>
            <apex:pageBlockTable value="{!proposalLineItemList}" var="proposalLineItem" styleClass="BlockTable">
                {!proposalLineItem.Product__c}
                {!proposalLineItem.UnitPrice__c}
                {!proposalLineItem.Quantity__c}
                <apex:column headerValue="Product" styleClass="ui-state-active">
                <apex:outputField value="{!proposalLineItem.Product__c}" styleClass="text{!index}"/>
                     <!--<apex:input  type="Number" id="Price" value="{!sec.salesprice}"  />-->
               <!-- </apex:column>
                <apex:column headerValue="Sales Price" styleClass="ui-state-active">
                   <apex:inputField type="text" id="Desc" value="{!proposalLineItem.UnitPrice__c}" required="true"/>
                                     
                 </apex:column>
                
                
                <apex:column headerValue="Quantity" styleClass="ui-state-active">
                <apex:inputField id="Price" value="{!proposalLineItem.Quantity__c}" styleClass="text{!index}" onclick="quantValid('{!proposalLineItem.Quantity__c}',{!index})" required="true"/>
                     <!--<apex:input  type="Number" id="Price" value="{!sec.salesprice}"  />-->
               <!-- </apex:column>-->
                
               <!-- <apex:variable value="{!index+1}" var="index"/>
                <apex:param name="index" value="{!indCount}"/> -->
                        <!-- <apex:variable value="{!indCount+1}" var="indCount"/>
               <!-- <apex:column headerValue="Action">
                    <apex:commandLink action="{!removeRow}" immediate="true" reRender="table">
                        <img src="{!$Resource.RemoveIcon}" width= "21"/>
                        <apex:param name="index" value="{!indCount}"/> 
                         <apex:variable value="{!indCount+1}" var="indCount"/>
                    </apex:commandLink>
                    
                </apex:column>-->
                 
           <!--</apex:pageBlockTable> <br/>           
            <center>
                <apex:commandButton value="Save" action="{!SavePage}" />
                <apex:commandButton value="Cancel" action="{!Cancel}"/>
            </center>
        </apex:pageBlock>
         <apex:outputPanel >
            <apex:pageMessages />
      </apex:outputPanel>  
    </apex:form>
</apex:page>-->
</apex:page>